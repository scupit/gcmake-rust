use serde::{Deserialize};

// TODO: Implement this. Right now it's just a placeholder so the empty OpenGL config doesn't fail.
#[derive(Deserialize)]
#[serde(deny_unknown_fields)]
pub struct RawBuiltinFindModuleDep {

}

